import images, { imageURL } from './images'
import icons from './icons'
import { COLORS, FONT, SIZES, SHADOWS } from './theme'
import TABS from './tabs'

export { images, imageURL, icons, COLORS, FONT, SIZES, SHADOWS, TABS }
